<h1>Pages#home</h1>

<%= form_with(model: @todo, data: { reflex: "submit->TodosReflex#create" }) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.submit class: 'btn btn-primary' %>
<% end %>


<%= render(TodoComponent.with_collection(Todo.all, todo_model: @todo)) %>
